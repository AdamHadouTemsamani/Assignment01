namespace MyApp;

public static class Generics
{   
    /*
    public static int GreaterCount<T, U>(IEnumerable<T> items, T x)
    where T : IComparable<T> 
    {
        foreach(var i in items)
        {
            if(i.CompareTo(x) > 0) yield return i;
        }
    }

    public static int GreaterCount<T, U>(IEnumerable<T> items, T x)
    where T : U
    where U : IComparable<U>
    {
        return 0;
    }
    */

}
